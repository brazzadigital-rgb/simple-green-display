/**
 * Vinho Premium header — essentially the current default header.
 * Re-exports StoreHeader since it already uses bordô/primary dark theme.
 * This exists as a named variant so the template system can reference it.
 */
export { StoreHeader as HeaderVinhoPremium } from "@/components/store/StoreHeader";
